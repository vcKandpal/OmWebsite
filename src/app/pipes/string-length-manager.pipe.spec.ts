import { StringLengthManagerPipe } from './string-length-manager.pipe';

describe('StringLengthManagerPipe', () => {
  it('create an instance', () => {
    const pipe = new StringLengthManagerPipe();
    expect(pipe).toBeTruthy();
  });
});
