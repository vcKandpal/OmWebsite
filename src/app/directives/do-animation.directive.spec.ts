import { DoAnimationDirective } from './do-animation.directive';

describe('DoAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new DoAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
